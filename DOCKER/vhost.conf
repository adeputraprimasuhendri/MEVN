server {
        listen 80 default_server;
        listen [::]:80 default_server;
        server_name _;

        root /var/www;
        index   index.html index.htm;
        server_tokens off;
        client_max_body_size 100m;
}